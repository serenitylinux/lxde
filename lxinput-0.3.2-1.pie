# vim: ft=sh
name="lxinput"
iteration="1"
version="0.3.2"
desc="A small program to configure keyboard and mouse for LXDE."
bdeps=('gtk2')
deps=('gtk2')
arch=('any')
flags=()


function configure() {
	default
}

function build() {
	default
}

function installpkg() {
	default
}
